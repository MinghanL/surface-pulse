{
  "name": "surface-pulse",
  "version": "1.0.0",
  "description": "Haptic feedback web app for iPhone/iPad — sends material & touch data via Bluetooth to MCU",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "gh-pages": "^6.0.0"
  },
  "dependencies": {
    "interactjs": "^1.10.27"
  }
}
